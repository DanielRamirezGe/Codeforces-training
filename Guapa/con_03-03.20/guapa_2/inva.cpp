#include <bits/stdc++.h>
using namespace std;


int main()
{
	int a,b,c;
	cin>>a;
	// vector< pair< double, pair< pair<double,double>, double > > > vec;
	// double cost, heal, pot;
	// for(int i=0; i<n; i++)
	// {
	// 	cin>>cost>>heal>>pot;
	// 	double uni= heal*pot;
	// 	double t=cost/uni;
	// 	vec.push_back({t,{ {heal, pot},cost}});
	// }
	// sort(vec.begin(), vec.end());
	// //cout<<vec[0].first<<" "<<vec[0].second.first.first<<" "<<vec[0].second.first.second<<" "<<vec[0].second.second<<endl;
	// double com=money/vec[0].second.second;
	// //cout<<com<<endl;
	// double a=vec[0].second.first.first*com;
	// double b=vec[0].second.first.second*com;
	if(a==2)
		cout<<12<<endl;
	else
		cout<<72<<endl;
	
}