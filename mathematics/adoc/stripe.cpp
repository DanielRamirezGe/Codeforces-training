#include <bits/stdc++.h>
using namespaces std;

int main()
{
	int cases, tam;
	cin>>cases;
	for(int i=0; i<cases; i++)
	{
		cin>>tam;
		cin>>num
		int suma=0, aux;
		for(int j=0; j<num; j++)
		{
			cin>>aux;
			suma+=aux;
		}
		suma+=(num-1);
		if(suma>tam )
			cout<<0<<endl;
		else
			{

			}
	}
}